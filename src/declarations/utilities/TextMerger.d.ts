declare class TextMerger {
    private static tempElementForEscape;
    private static mergeValues;
    static mergeText(text: string): string;
    private static buildMergeValues;
    private static escapeHtml;
}
